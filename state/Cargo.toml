[package]
name = "state"
version = "0.1.0"
edition = "2021"
description = "Deterministic in-memory state and snapshot/restore utilities for MCRCoin v1"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
bincode = "1.3"
anyhow = "1.0"
sha2 = "0.10"
hex = "0.4"
