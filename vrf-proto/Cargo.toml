[package]
name = "vrf-proto"
version = "0.1.0"
edition = "2021"

[lib]
name = "vrf_proto"
path = "src/lib.rs"

[build-dependencies]
prost-build = "0.11"
protoc-bin-vendored = { version = "3.24", optional = true }

[dependencies]
prost = "0.11"
prost-types = "0.11"
