[package]
name = "evidence"
version = "0.1.0"
edition = "2021"
description = "Evidence manager: detect and record equivocations/double-signing for MCRCoin v1"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
anyhow = "1.0"
sha2 = "0.10"
hex = "0.4"
